#include "Events.h"

/* bool EventsManager::event(QEvent* event)
{
if (event->type() == QEvent::Type(QEvent::User + 1)) {
    std::cout << "\nGotcha Event - Pass 1";

    return true; // Event was handled
}
return QObject::event(event); // Pass unhandled events to the base class
} */
